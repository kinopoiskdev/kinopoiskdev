import { commonRepositories } from './common-repositories';

describe('commonRepositories', () => {
  it('should work', () => {
    expect(commonRepositories()).toEqual('common-repositories');
  });
});
